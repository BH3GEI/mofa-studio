[package]
name = "mofa-hello-world-rust"
version = "0.1.0"
edition = "2021"
description = "Hello World example app with a Rust web server"

[dependencies]
makepad-widgets = { workspace = true }
mofa-widgets = { path = "../../mofa-widgets" }
log = "0.4"
serde_json = "1.0"
